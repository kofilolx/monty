#include "monty.h"

/**
 * m_stack - prints the top
 * @head: stack head
 * @line_counter: line_number
 * Return: no return
*/

void m_stack(stack_t **head, unsigned int line_counter)
{
	(void)head;
	(void)line_counter;
	
    bus.lifi = 0;
}